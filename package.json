{
  "name": "unloop-mcp",
  "version": "0.1.0",
  "description": "MCP Server that detects AI fix loops and provides escape strategies",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "unloop": "dist/bin/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "test": "NODE_OPTIONS='--experimental-vm-modules' npx jest"
  },
  "keywords": ["mcp", "ai", "loop-detection", "coding-assistant", "vibe-coding"],
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.5.0",
    "jest": "^29.7.0",
    "@types/jest": "^29.5.0",
    "ts-jest": "^29.1.0"
  }
}
